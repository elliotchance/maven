namespace maven;

/**
  Thrown only in extreme cases when if not thrown would otherwise cause the
  program to crash. Such situations include:
   * Attempting to use an Object that is nil.
 */
class ObjectNilException extends Exception {
	external public ObjectNilException();
	external public ObjectNilException(maven.String variable, maven.String filename, int occuredLine);
}
